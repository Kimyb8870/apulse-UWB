import React from 'react';
import { MapName, StyledMapInfo } from './MapInfoStyles';

const MapInfo = () => {
  return (
    <StyledMapInfo>
      <MapName>MapName: Apulse</MapName>
      {/* todo: Map 관련 info를 확인할 수 있는 구역, 기능 및 디자인 추가 기획 필요함 */}
      추가기획 이후에 상세구현
    </StyledMapInfo>
  );
};

export default MapInfo;
